version: '3.8'

services:
  db:
    image: postgres:13.0-alpine
    volumes:
      - /var/lib/posgresql/data/
    environment:
      POSTGRES_PASSWORD: qwerty

  api:
    build: .
    ports:
      - 8000:8000
    depends_on:
      - db
